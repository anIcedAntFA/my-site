import { defineKeyframes } from '@pandacss/dev';

export const keyframes = defineKeyframes({
	'shiny-glass': {
		'0%': { left: '-100%' },
		'50%': { left: '120%' },
		'100%': { left: '120%' },
	},
});
